package it.polimi.ingsw.ps45.model.actions;
/**
	 * Modes in which a production of a card can be executed on.
	 * NONE: no production is executed
	 * FIRST: the first production is executed
	 * SECOND: the second production is executed
 */
public enum ProductionMode {
	NONE, FIRST, SECOND;
}
